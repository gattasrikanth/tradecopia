# Web

The first Alpha dashboard is the local-only SPA served from
`src/ControlPlane/TradeCopia.ControlPlane/wwwroot`.

No CDN, no remote fonts, no telemetry. A future Vite/React rewrite can replace
those files in place if the same loopback API is preserved.
