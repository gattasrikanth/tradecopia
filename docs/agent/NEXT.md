# Next actions

1. Keep `main` green after this Alpha/RC checkpoint.
2. Raise domain coverage toward 95/90 without excluding safety logic.
3. After the owner launches NinjaTrader once: `scripts/install-local.ps1` and manual SIM matrix.
4. Named-pipe OS transport on top of ProtocolSession (in-process session is tested).
5. Do not label Stable.
