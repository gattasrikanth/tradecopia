# Next actions

1. Keep `main` green (CI parse/scripts + tests).
2. Named-pipe engine ↔ control-plane handshake (no ExecuteOrder message).
3. Raise domain coverage toward 95/90.
4. SIM-only native submit path behind positive simulation detection.
5. Playwright critical flows + synthetic screenshots.
6. After the owner launches NinjaTrader once, run `scripts/install-local.ps1`.
