# Upgrade

Run the newer `TradeCopia-Setup-*.exe`. Setup stops the companion, requires NinjaTrader closed if native files change, and replaces `%LOCALAPPDATA%\TradeCopia\app`. Journal data under `data\` is preserved.

Copying remains disabled after upgrade.
